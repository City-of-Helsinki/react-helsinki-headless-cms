fragment listVenue on Venue {
  description
  id
  image
  name
  streetAddress
  ontologyWords {
    id
    label
  }
}

query VenuesByIds($ids: [ID!]!) {
  venuesByIds(ids: $ids) {
    ...listVenue
  }
}
