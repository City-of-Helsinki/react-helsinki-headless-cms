query pages($first: Int) {
  pages(first: $first) {
    edges {
      node {
        id
        title
        date
        lead
        slug
        uri
        featuredImage {
          node {
            altText
            mediaItemUrl
          }
        }
      }
    }
  }
}
