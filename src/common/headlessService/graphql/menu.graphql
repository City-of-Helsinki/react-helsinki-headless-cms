fragment MenuItem on MenuItem {
  id
  order
  target
  title
  url
  label
}

query menu($id: ID!) {
  menu(idType: NAME, id: $id) {
    id
    menuItems {
      nodes {
        ...MenuItem
      }
    }
  }
}
