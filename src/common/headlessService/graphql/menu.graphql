fragment MenuItem on MenuItem {
  id
  order
  target
  title
  path
  label
}

query menu($id: ID!) {
  menu(idType: NAME, id: $id) {
    id
    menuItems {
      nodes {
        ...MenuItem
      }
    }
  }
}
