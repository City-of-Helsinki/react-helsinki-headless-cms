query posts($first: Int) {
  posts(first: $first) {
    edges {
      node {
        id
        title
        date
        lead
        slug
        uri
        featuredImage {
          node {
            altText
            mediaItemUrl
          }
        }
      }
    }
  }
}
